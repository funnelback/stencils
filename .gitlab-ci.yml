image: docker-hub.squiz.net/stencils/stencils:latest

build:
    cache:
        key: ${CI_COMMIT_REF_SLUG}
        paths:
            - /root/.gradle
    script:
        - ./gradlew build
    tags:
        - docker
