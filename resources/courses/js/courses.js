/*
  Stencil: Courses
  Description: Scripts for courses stencil
*/

var stencils = typeof stencils == "object" ? stencils : { module : {} };
stencils.module.courses = (function ($, window, stencils, undefined) {
/*
	Sub Modules
*/

/*
  Runtime logic and event bindings
*/
  //On DOM Ready
  $(function () {

  });

}(jQuery, window, stencils) );
