build FB master:
    image: docker-hub.squiz.net/funnelback-rnd/gitlab-ci-docker:stencils-master
    cache:
        key: ${CI_COMMIT_REF_SLUG}
        paths:
            - /root/.gradle
    script:
        - ./gradlew build
    tags:
        - docker
        - shared
